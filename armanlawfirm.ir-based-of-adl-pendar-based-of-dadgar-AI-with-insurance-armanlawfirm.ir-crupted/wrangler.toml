# Cloudflare Pages Configuration
# All-in-one deployment: Frontend + Backend Functions

name = "armanlawfirm"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[vars]
NODE_VERSION = "20"

[build]
command = "npm install && npm run build"

[build.environment]
NODE_VERSION = "20"
